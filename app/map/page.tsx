import { MemoryTideShell } from "@/components/memory-tide/MemoryTideShell";

/** Spatial travel memory map (Yunnan) — separate from home album at `/`. */
export default function MapPage() {
  return <MemoryTideShell />;
}
